Using this weatherApp, application u can get the temperatures of the city, that u searche for.

Used an api from openweathermap. 

To run this, 
clone the repo, and just run `index.html` file in a web browser.

